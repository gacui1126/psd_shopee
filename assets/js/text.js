const main = document.querySelector(".home-page__banner-main");
const link = document.querySelector(".home-page__banner-main-link");
const img = document.querySelectorAll(".mySlides");
const imgWidth = img[0].offsetWidth;
const sliderLength = img.length;

console.log(sliderLength);